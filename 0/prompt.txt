we recently merged some VR is that made the recommended Python setup much more opinionated. I'd like to make sure that we've appropriately called out the command line flag to override this check for users that know what they're doing and want to keep doing things the whatever way they have it set up. That is that's okay as long as they know what they're doing. So highlight the various areas that we could mention this. for example, in the appropriate places where we recognize there is an existing Python setup and maybe it works. If it can't possibly work, don't show the flag. But if it could work, but it's not up to our recommended settings, suggest where we could call out this flag. This should be both in the HCLI output as well as in the website documentation in those markdown files. 

--no-python-environment-check