We've recently added this functionality for better detecting the virtual environment used for Ida. And we have this doctor command for example. And I'm wondering about the interplay of when the HCLI specific environment variable, it's like HCLI current Python executable, something like that. Um and that like overrides the IdaPython VNv executable virtual uh environment variable. Like how that interplay uh is handled. I had one user who had previously set the HCLI specific. Environment variable mistakenly. And then that wasn't like being referenced in the doctor output. So can you audit how that is being handled today and whether or not we also need some extra handling to identify if this highly specific environment variable is also being set uh and at least and at least warning the user that like the behavior is different because of set. that they should probably not set it. Unless they need HCLI specific behavior, which is almost never the case. So it should probably discourage users from doing anything with that. Because Ida's not going to use that variable So go ahead. Again, go ahead and research the behavior today and come back with a concise report. ASD STE 100 to help explain like well what is this scenario today and what might we need to do