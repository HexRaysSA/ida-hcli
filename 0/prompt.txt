Okay. I actually expect that they should be supported through repo add with the bundle path. I would expect this to be the case. And if not, it's probably a bug in our implementation. So kick off some research to confirm that path. We want this to work. Does it work? If so, let's document it as such, like I originally described. If not, highlight why it doesn't work today and we'll address this in another implementation pass.