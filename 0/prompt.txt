Okay, so we recently introduced functionality for there to be tightly bundled plugin components, which is basically a parent plugin bundling Sub plugins that are all installed together. And I think we have handling for, you know, once we install one of these, you know, bundles to install the dependencies for the root plugin as well as for the Uh children plugins. First, confirm this. Make sure that Python dependencies for all of these are installed. And that's recursively. So if a bundled plugin has another bundled plugin, Python dependencies are installed for all of them. First confirm that. Now the second thing I'm concerned about is that we may not be uh configuring setting the settings, updating the configuring the con the configuration for the the bundled plugin. components. I think maybe this is only running for the root plugin. So I also want you to audit this. And of course this should be done recursively as well, where we're prompting the user for settings values for all the components recursively. And and furthermore, we probably want to be accepting uh via command line flags at the top level installation config values that might be used by the components recursively. So audit whether this is already taking place or whether this is something that we need to add