All right. I want to do a research spike to understand The considerations, the design decisions, the architecture, the things that need to change specifically. In order to support IdaPro plugins that depend on other IdaPro plugins, using the Hexrays plugin manager and HCLI. I'll share with you the documentation for how plugins are packaged today and what that looks like. What we need, and we need With high priority is twofold. One in order to support upcoming extensions to Ida That are that are private, that are paid for, distributed by Hexrays for money, through sales. Fundamentally and technologically, under their hood, they're a collection of players. plugins, each providing a new feature to Ida, and we sell the the collection of them. And we need users to be able to Download the set of them, install them, subsequently upgrade them, or remove them as a whole group Not piecemeal. And in fact users shouldn't even really be aware that they are plugins, just that they have new functionality available in Ida once they've maybe um accomplished a single step or invoked some tool or installer. We have a mechanism for distributing plugins today. We have this plugin manager, we have a private plugin repository served by the Hex X-rays portal. This is authenticated and access to plugins is provided using the account details for the logged in authenticated user so we can gate access to the plugins appropriately. That's all in good shape. But this first thing that we need is the ability to group plugins together and operate them on them as sets. And that means we install them together, we uninstall them all together, and they're all uninstalled, and we upgrade them together And it's not possible to go in and upgrade one or two of them or manipulate individual members of the group Because they move as a collection. And possibly this collection or this group is recursive. I don't know. That's something that we'll have to research. But we need to be able to kind of refer to the group which kind of delegates to individual plugins under it. So that's the first thing that we need The other thing that we want, this isn't as required and isn't as a high priority right now, but seems like we might as well address it right now. while we're at it is the idea that we should be able to install a collection of plugins By a single name, and this should serve as a shortcut to acquiring a collection of of potentially related plugins. You know, for ease of use, configuration, and discoverability. So for example, an analyst that's analyzing programs written in Go might install the Go analysis path. And this might install a bunch of plugins from the community plugin repository. And in by installing this one, they get access access to four or five underlying plugins, maybe to extract of types or fix strings or annotation. the database or whatever. And what's different about this is that it's really just a shortcut to installing those plugins. And that after the fact, I don't know if there's an association. Or how strong the association is between the pack and the underlying plugin. Like we should be able to upgrade the underlying plugins without without upgrading the whole pack, I think. And if we uninstall one of them, the other ones should kind of still be there. Like those plugins kind of retain their identity and their individuality. I don't know what it means for the meta plugin to be uninstalled without the other ones being installed, um, or if there's some scenario where there's like version conflicts. I'm not exactly sure on that. But this is, you know, this stands in contrast to the groups of plugins that move together. Those can have versions that are specifically pinned and always move together very very like in a very in lockstap. And then we have this other kind of category of uh plug-in dependencies that um are a bit more loose, their shortcuts. They can still be required. At least in order to install like the the meta plugin, we can still require the installation of those dependency plugins. But then after the fact, I don't know if they still need to, you know, we need to assert that they're still required or exactly what that looks like In theory, we could also probably have optional or recommended plugins, but it's kind of unclear to me what the user experience there looks like I don't think we want to present the user with a lot of like click buttons. Do you want to install this? Do you want to install this? Yes, no, yes, no. A lot of um Sounds like that adds a lot of noise. So what I'd like you to do is take a look at the documentation for the format for the the plugin manager today How this works. I also want to take you to take a look at this document that explains background research on how other systems do plugins. And then I want you to come up with three concrete proposals on how we could implement this. I'm generally imagining adding New parts to the IDA plugin. json file and extending it in different ways to specify you know these sorts of things. And I want you to come up with between three and five proposals that kind of show a variety of ways to make this happen for example like maybe there's some sort of new top level key indicating this plugin is like a meta plugin or maybe uh there there's various links. Um one thing we do have to keep in mind is like when we're dealing with um you know the class one sort of plug-in groups that all have to move together. Um do these things like get segmented in their own subdirectory maybe? Uh in order to kind of keep the Ida user plugins directory um A bit more organized and tidy. How do these things influence um the outputs of commands like if we're listing plugins and a plugin is part of a group or I guess we only probably show the group um And so we have all these kind of considerations. I want you to be kind of carefully study this very thoroughly. Think about this. I want you to Take as much time as necessary to turn over all these different ideas, and then we'll go back and forth and kind of discuss the pros and cons and come up with a nice design

  https://hcli.docs.hex-rays.com/user-guide/plugin-manager/
  https://hcli.docs.hex-rays.com/reference/plugin-repository-architecture/
  https://hcli.docs.hex-rays.com/reference/plugin-packaging-and-format/
  https://hcli.docs.hex-rays.com/reference/packaging-your-existing-plugin/
  https://hcli.docs.hex-rays.com/reference/plugin-bundle-spec/

/Users/user/Obsidian/hex-rays/hcli/plugin dependencies and packs.md