Yeah, we're only d describing. Tight coupling right now. And there's an additional thing that we need to do, which is that for all of the the members of the larger group, we also need to install the dependencies of those. And that's even potentially recursive. So I I still like this idea of bundling physically together all of the uh members of the the tight bundle. Um but we do need to appropriately update HCLI so that it one has references from the parent to the children, uh and that can be via the components key. It just be it needs to be the name of the well it really should be the name of the subdirectory and that should match the plugin name in the Ida plugin JSON in that subdirectory. directory uh and that should be the link from the parent to child. Uh and then it also knew you also need to install uh dependencies appropriately. um and update them appropriately when we um work with that tightly coupled bundle. And yes, of course, like you mentioned, we also have to make sure that um there's no name collisions. And we also, as I mentioned, have to handle the recursive case where we might have like recursively nested plugins. And the loose packs we have a good design for this. um we can call this you know dependencies or something like that. Uh in these this is kind of the loose setup and we also have a packed setup too.