Okay, when we have created a Python environment for the user. Um and I think it's not when they are installing Ida, but when they use the HDLI IDA Python create environment command. This may be to migrate from some existing Python setup. And there is an edge case where the user may already have plugins installed. And therefore, changing the Python environment might disrupt those plugin installations. Now the plugins themselves are stored in Idausers plugins and those remain there. O's are still available. However, if any of those have Python dependencies, those would have been installed in the Python environment. And so what we'll want to do is prompt to install all of those Python dependencies. So first, poke around and make sure you understand what I mean. Take a look at what how we handle installing a plugin. And then look at how we create the virtual environment. and recognize that after we've created the virtual environment, we could prompt the user and say, these are the dependencies that are required for your currently installed plugins. Do you want us to install them? And we can prompt the user, and if they say yes, then we should ensure that those Python dependencies are now available in the newly created virtual environment. Now I think there's yet another edge case where maybe some of these packages aren't available. Um and so this is obviously going to be a problem. We can call out to the user maybe which plugin fails. uh will not have its um dependencies available and we may have the user to ask them ask them to to reinstall it from whatever source they got it from. Uh if the if it's these packages aren't available you know on the public PyPy repository, then maybe that it's like an internal plugin um or maybe they got it from some other like offline source. Uh and so in this scenario we will like we're probably not going to be able to fix it, but we can at least tell them what failed. Now I think what we'll want to do is actually like collect all of the plugins uh or d rather the Python dependencies that need to be installed, collect them together and try to do install them in one big um pip install command. Um but if one of those like the dependencies fails to install, we might have to actually go back and go plug-in and buy plugin uh and install the dependencies. Um or maybe we should do that from the start. I don't really know. Use your best judgment there, but we want to be able to give users feedback that you know plug-in by plug-in, they will be able to reinstall the dependencies into their virtual environment, and things ideally should be good to go. So, first, take a look around, make sure you understand what I'm talking about. Regurgitate it back to me and then grill me to make sure we have a clear picture of what's required