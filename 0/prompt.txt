Okay, now I want you to do a thorough review of the code base to identify all the areas that need to change in order to uh implement this plan. I I specifically want a lot of really simple, easy to read and assess helpers for detecting the environment. For example, is Windows is Mac OS. Is Linux? Is Wayland? Is X eleven? is fish shell, is Z H shell, etcetera. All of the appropriate checks that we're going to need to detect the environment, we need really concise um accessors for this. And then in our code that decides what to do, we need a very easy to review decision tree that encodes our table. from above. Now, in terms of the user flow, when the user creates the environment, and we get to the moment when the environment environment variable needs to be set. Then what needs to happen is HG Lai needs to show a plan of action. Specifically, what is going to be done and then prompt the user to say do you want to do this? Yes or no. If they say yes, we go ahead and make the changes necessary to write the environment variables into the right places. Of course, we'll need to also take into account that maybe there might already be some values there, so we might need to update them. I don't know if how that interplays with us already detecting whether or not the environment variable is set, but we need to take that into account. If the user says they don't want to automatically set these configuration values, then we need to indicate to them exactly what they need to set on their system using whatever manual technique they want to use to set IdaPython VNV. executable to point to that path. And so again, we're going to show this plan of action with the specific sets, like the specific steps, like create this file with this contents, then we're going to invoke this command. Um do you wanna do this? And then I if they actually go ahead with this, then um if we expect the environment variable to take effect right away, we should actually check and see is that now available? Um using an appropriate mechanism. I don't know if like we can spawn a subprocess to read the current environment and whether or not that's gonna be propagated down. Ah correctly. If this is straightforward to do, we should try to do that to confirm it took effect. But if it's not going to take effect just like that and there are re like a login or restart is required, again, we need to tell the user about that. But we want this to be extremely smooth, the user needs to know what is happening. And have a just chance to, you know, do things on their own or to bail, of course. um but also for it to be extremely smooth and work in all the environments. And finally our code needs to be so easy to audit to understand exactly how are we checking these things. Because I anticipate we'll have a number of users that have strange Linux setups. where we don't quite detect things correctly. Or for example I'm running fish shell on Mac OS, maybe I don't expect this to be supported. I don't know if this wheeler won't work. Uh and so we need to also recognize these cases. be able to either identify what behavior the user encountered if they have a bug report or how to extend this further if we want to support further and further edge cases. So go ahead now, look at the code, figure out exactly what we need to update and where. So we do this in the right place in the surgical manner and have this really clean code. Once you have a good understanding of that, in a new work tree, go ahead and implement these changes, creating a new PR.