read:
- /Users/user/Obsidian/hex-rays/hcli/Proposal IDAPython Environment Consistency.md
- /Users/user/Obsidian/hex-rays/hcli/Persistent, Per-User, Login-Time Environment Variables Across Platforms.md

Okay, so you'll see from these two documents that we are proposing to simplify the way that users configure Ida to reference a Python virtual environment and the mechanism that they do that is via an environment variable The reason I think that we need to do this today and in this way using environment variables is because we need to support existing versions of IDA. Like 9. 0, 9. 1, all the way through 9. 4. Now it's possible that in Ida 9. 5 we could cause it to look into The Idaconfig. json file and use a key here to specify the virtual environment to load and to do this before it actually loads the Python interpreter. This is possible maybe from 9. 5 or 9. 6 onwards but does not address the users who are currently using IDA 9. 4 or below. So that's why we've chosen environment variables to specify the virtual environment. Alternative mechanisms that we could use to do this include updating the Ida. python RC file, which is arbitrary Python code And we reference this in the document and is tricky to programmatically work with. Ida PySwitch doesn't take into account all virtual environments, so that doesn't work. I think this is what we have to work with. So this is why we're using environment variables in this proposal. Looking forward, we concur that We might want to move to a better supported mechanism that's also more consistent across platforms, like that configuration file So maybe that's something that happens kind of in parallel and for IDA 9. 5 and above. And then over time we can remove support for this environment variable thing in A. HCLI. So this seems like a appropriate thing to do. But in the short term We need to support IDA 9. 4 and maybe IDA 9. 5 with this technique. of using environment variables. And so furthermore, let's speak out exactly what our requirements are. We're talking Windows, Mac OS, and Typical reasonable distributions of Linux. And within these cases, we want to support both and primarily graphical users of IDA who invoke IDA via the doc or an icon or the start menu, etc. And that Ida needs to load their virtual environment. And so the environment variable needs to be present in the graphical desktop session um on Linux. You know, this might be Wayland, this might be X It might be from GNOME, KDE, XFCE, whatever. And we want to do a kind of a have reasonable support for this. We also need to support terminal users, people who might log in via SSH. uh to a host uh and invoke Ida via Ida lib uh and so we also need to support those sorts of things And it's fine if we generally support like for example dot profile or or similar there. On Windows, I'm not sure if this is relevant or not, but let's keep kind of keeping keep it in mind. So with this in mind, with these requirements, with this background in mind, again, read those two documents, come up with You know, I concise proposal and any kind of um concerns that we need to take in mind. into mind uh so that we can do a couple things. One, we can update our documentation, like this proposal document, to make very clear why we're doing it, what we're doing it. Don't edit these files directly, just read from them, but highlight to me what we should be kind of saying in them. That's because I have another Google document. with this content. So I'll highlight the additional kind of things we'd want to include in that document so readers understand where we're coming from and why we're doing it in this way. um maybe what our future plans are and then also come up with specific guidance on what are the mechanisms that we use to set these environment variables on a per platform basis. We want to prefer using the most idiomatic techniques We want to prefer using kind of like dedicated application-specific files when possible. And also, when possible, we want HCLI to kind of like prompt the user and say, hey, I can do this thing. Do you want me to do it? If so, press yes. And if they don't agree, we can explain to them what they might need to do or always give them the chance to do it on their own. So go ahead, read through these documents, read, consider my background context. And propose again, what do we need to change in our documentation? And then what are the concrete ways that we should be setting environment variables on major platforms?