Okay, so we've discovered that Ida will actually resolve plugins that are nested kind of within each other. So there can be a top-level plugin and then a subdirectory that contains another plugin and Ida will recognize both of those and will load all of them. And so this to me begs the question of why do we build in specialized logic into HDLI? For the case of these tightly coupled plugin bundles, plug-in collections, plugin packs, the tightly bundled ones. Maybe what we should do here is just support nested plugins Because this way We don't have to have all the extra infrastructure of like, okay, so we resolved the top level plugin and then now we have to go fetch these other ones and install install those into this other directory, do all this work. We could just unpack the top level archive. Is this sufficient? And then that top level archive doesn't actually necessarily eat even have to have references into its children. Now perhaps it could it can and perhaps it should because I think we already have checks within HGLI already to disallow multiple ida plugin. json files in the same archive and in the same directories or something. So maybe we need to loosen up that that logic somehow and maybe support this in a way. And so I'd like you to kind of consider what I've suggested, consider the implications of this, make sure you understand and highlight issues with what I've just described.