<task-notification>
<task-id>aa7079a4b08080bd2</task-id>
<tool-use-id>toolu_01C4KFt1qi8FkWZrjhXeZpvo</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-user-code-hex-rays-ida-hcli/099ceea2-439b-4b7d-80c8-2cfb47ac298d/tasks/aa7079a4b08080bd2.output</output-file>
<status>completed</status>
<summary>Agent "Read HCLI source files" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Written to `/tmp/source-summaries.md`. Summary of findings:

**python.py** (~940 lines): The core complexity. Spawns IDA as a subprocess to probe its Python environment, then runs an 85-line heuristic (`_derive_python_exe`) to derive the actual interpreter path from `sys.prefix`, `sys.base_prefix`, `sys.executable`, `$VIRTUAL_ENV`, and `$IDAPYTHON_VENV_EXECUTABLE` — with special cases for Windows Store shims, macOS framework Pythons, IDA 9.4+ macOS (where `sys.executable` is idat itself), and PEP 668 externally-managed environments.

**venv.py** (~300 lines): Virtual environment detection utilities. Notable complexity around discriminating uv ephemeral cache environments from real user venvs (three independent heuristics), plus version probing with fallback from running the interpreter to reading pyvenv.cfg.

**explain_environment.py** (~700 lines): A diagnostic command that collects and renders a comprehensive environment report. Exists as a troubleshooting tool because the environment has so many moving parts. Generates warnings for uv overlay confusion, HCLI venv misidentification, missing venv activation, PEP 668, version mismatches, and EOL Python.

**Skill document**: Codifies the "happy path" — use a venv at `~/.idapro/venv`, register it with idapyswitch, set `IDAPYTHON_VENV_EXECUTABLE`. If everyone followed this, most of the detection/fallback code would be unnecessary.</result>
<usage><subagent_tokens>62833</subagent_tokens><tool_uses>7</tool_uses><duration_ms>79731</duration_ms></usage>
</task-notification>