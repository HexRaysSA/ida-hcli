please create some tests to show this behavior working. We should test. when no migration is needed, when a migration is needed, and when a migration has an unmigratable plugin. We need to make sure we have test coverage for this behavior because it's a little bit gnarly the way that we invoke pip and we have these new environments and all that sort of stuff. So take the time to create concise, straightforward tests that demonstrate the behavior without using mocks or fakes or monkey patching.