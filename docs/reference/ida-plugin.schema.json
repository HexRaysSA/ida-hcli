{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IDA Plugin Metadata Schema",
  "type": "object",
  "required": ["IDAMetadataDescriptorVersion", "plugin"],
  "properties": {
    "IDAMetadataDescriptorVersion": {
      "type": "integer",
      "enum": [1],
      "description": "Version of the metadata descriptor. Must be 1."
    },
    "plugin": {
      "type": "object",
      "required": [
        "name",
        "entryPoint",
        "categories",
        "logoPath",
        "idaVersions",
        "description",
        "version"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The name will be used to identify the plugin and also generate a namespace name for it if necessary (e.g., an IDAPython plugin). The namespace name is generated by converting all non alphanumeric characters of the plugin name to underscores (_) and prepending __plugins__ to it. For example \"my plugin\" would become __plugins__my_plugin."
        },
        "entryPoint": {
          "type": "string",
          "description": "The filename of the \"main\" file for the plugin. It should be stored in the same directory as its ida-plugin.json file. If the entryPoint has no file extension, IDA will assume it is a native plugin and append the appropriate file extension for dynamic shared objects for the host platform (.dll, .so, .dylib). For IDAPython plugins, this should typically be a .py file (e.g., my-first-plugin.py)."
        },
        "categories": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "enum": [
              "disassembly-and-processor-modules",
              "file-parsers-and-loaders",
              "decompilation",
              "debugging-and-tracing",
              "deobfuscation",
              "collaboration-and-productivity",
              "integration-with-third-parties-interoperability",
              "api-scripting-and-automation",
              "ui-ux-and-visualization",
              "malware-analysis",
              "vulnerability-research-and-exploit-development",
              "other"
            ]
          },
          "description": "At least one category for discoverability."
        },
        "logoPath": {
          "type": "string",
          "description": "Include an image to visually represent your plugin on its page at plugins.hex-rays.com. This should be a relative path to an image file within your plugin’s repository. The recommended aspect ratio for the image is 16:9."
        },
        "idaVersions": {
          "type": "array",
          "description": "List of specific IDA versions that your plugin supports. Each version must be declared explicitly.",
          "items": {
            "type": "string",
            "enum": [
              "10.0", "9.4", "9.3", "9.2", "9.1", "9.0sp1", "9.0", "8.5", 
              "8.4sp2", "8.4sp1", "8.4", "8.3", "8.2sp1", "8.2", "8.1", "8.0sp1", "8.0",
              "7.7sp1", "7.7", "7.6sp1", "7.6", "7.5sp3", "7.5sp2", "7.5sp1", "7.5",
              "7.4sp1", "7.4", "7.3", "7.2", "7.1", "7.0sp1", "7.0", "6.95", "6.9",
              "6.8", "6.7", "6.6", "6.5", "6.4", "6.3", "6.2", "6.1", "6.0",
              "5.7", "5.6", "5.5", "5.4", "5.3", "5.2", "5.1", "5.0",
              "4.9sp", "4.9", "4.8", "4.7", "4.6", "4.x", "3.x"
            ]
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "description": {
          "type": "string",
          "description": "Summary of the plugin's functionality."
        },
        "version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "description": "Plugin version in x.y.z format."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
